# TODO

- Configure `VENICE_API_KEY` and `MORPHEUS_API_KEY` in Vercel.
- Add stable machine-readable benchmark adapters when source APIs permit redistribution.
- Expand canonical alias map for every live provider model.
- Add model detail routes and time-series snapshots.
- Add provider-specific price comparison where one model exists on both providers.
- Add automatic change report when a provider adds/removes a model.
