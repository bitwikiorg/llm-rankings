# TODO

- Verify every tracked exact-model benchmark row against its direct source and update stale/incorrect measurements.
- Configure `VENICE_API_KEY` and `MORPHEUS_API_KEY` in Vercel for live provider-catalog reconciliation.
- Expand canonical aliases for every live Venice and Morpheus text model.
- Add stable machine-readable benchmark adapters where source APIs permit redistribution.
- Improve source-specific normalization as evidence coverage grows; keep methodology explicit and versioned.
- Add provider-specific price comparison when the same model exists on both providers.
- Add automatic change reporting when Venice or Morpheus adds/removes a text model.
- Add historical snapshots only if they remain secondary to the single-page ranking surface.
