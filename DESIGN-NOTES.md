# Design direction

The interface is a provider-filtered research terminal, not a decorative tier list. Primary rank views remain source-native (Arena, Artificial Analysis, task benchmarks). Derived consensus is secondary and must never impersonate a source-native ranking.
